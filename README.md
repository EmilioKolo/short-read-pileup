# Short-reads pileup pipeline

Pipeline to find variants in an amplicon with as few filters as possible


Quick start:
```bash
bash run_pipeline.sh -1 <R1.fastq[.gz]> [-2 <R2.fastq[.gz]>] -r <ref.fasta> -a <aligner> -o <outdir> [-t threads] [--regen_docker_image]
```
